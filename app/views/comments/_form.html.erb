<%if user_signed_in?%>
  <%= simple_form_for([ track, comment ], remote: true) do |form| %>
    <%= form.input :content, placeholder: 'Leave a comment', label: false %>
    <%= form.button :submit, value: 'Post' %>
  <% end %>
<% end %>
