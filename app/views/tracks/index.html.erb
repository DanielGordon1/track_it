<%= render 'shared/collapse' %>
<%= render 'shared/header' %>
<%= render 'shared/sub' %>
<%#= render 'shared/sounds' %>

 <h1>Hot tracks</h1>

  <% @tracks.each do |track| %>
    <h2><%= track.tracker.username %></h2>
    <h2><%= track.name %></h2>
    <h2><%= track.version %></h2>
    <h2><%= track.genre %></h2>
  <% end %>
